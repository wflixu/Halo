import {
  "moonbitlang/async/http" @async_http,
  "moonbitlang/async/socket" @socket,
}

options(
  targets: {
    "request.mbt": ["native"],
    "response.mbt": ["native"],
    "server.mbt": ["native"],
  },
)
